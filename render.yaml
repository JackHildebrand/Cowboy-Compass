services:
  - type: web
    name: cowboy-compass-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python prepare_render_data.py
    startCommand: python api.py
    healthCheckPath: /health
    envVars:
      - key: ALLOWED_ORIGIN
        sync: false
